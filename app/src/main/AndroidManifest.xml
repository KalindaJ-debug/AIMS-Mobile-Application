<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.javamobileapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar">
        <activity android:name=".DataEntrySuccessful"></activity>
        <activity android:name=".LandDataEntryActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ApprovalDescription" />
        <activity android:name=".ApprovalMain" />
        <activity android:name=".Officer_logged" />
        <activity android:name=".officer_login" />
        <activity android:name=".farmer_login" />
        <activity android:name=".DataEntryActivity" />
    </application>

</manifest>